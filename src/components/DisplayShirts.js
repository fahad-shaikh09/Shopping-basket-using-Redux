import React from 'react'
// import { useSelector } from "react-redux"
// import ShowList from "./ShowList"
// import {useDispatch} from "react-redux";


const DisplayShirts = () => {

    // const id = useSelector(state => state.id)
    // const title = useSelector(state => state.title)
    // const itemPrice = useSelector(state => state.itemPrice)
    // const desc = useSelector(state => state.desc)
    // const color = useSelector(state => state.color)
    // const src = useSelector(state => state.src)
  
  
    // const priceTotal = useSelector(state => state.priceTotal)
    // const quantity = useSelector(state => state.quantity)
    // const itemsInCart = useSelector(state => state.itemsInCart)
  

    return (
        <div>
           <h1>DisplayShirts component</h1>

        </div>
    )
}

export default DisplayShirts
